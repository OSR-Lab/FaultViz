# FaultViz
Dynamically visualize fault injection results and produce fault injection plots.

## Installation
Requires a JDK to be installed with the `JAVA_HOME` environment variable configured.

## Usage

See `example.ipynb`.
